<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.uan.capitalapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_img"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_img2"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AgendaDeGastos"></activity>
        <activity android:name=".Menu"></activity>
        <activity android:name=".Login"></activity>
        <activity android:name=".Registrar"></activity>
        <activity android:name=".Tips"></activity>
        <activity android:name=".HorasExtra"></activity>
    </application>
</manifest>